# MLS Sekolah

Next.js JavaScript App Router dengan database JSON lokal di `data/db.json`.

- UI utama ada di `app/page.js` dan menggunakan bahasa Indonesia.
- API route berada di `app/api`.
- Jalankan dengan `npm install` lalu `npm run dev`.
